services:

  web_server:
    depends_on:
      - db
    restart: always
    build:
      context: ./src
      dockerfile: Dockerfile
    image: web_server
    ports:
      - 3000:3000
    networks:
      - db
    environment:
      - PORT=${PORT}
      - MYSQL_HOST=${MYSQL_HOST}
      - MYSQL_USER=${MYSQL_USER}
      - MYSQL_DATABASE=${MYSQL_DATABASE}
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
      - SECRET=${SECRET}

  db:
    restart: always
    image: mysql
    volumes:
      - /var/lib/mysql
      - ./epytodo.sql:/docker-entrypoint-initdb.d/schema.sql
    environment:
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
      - MYSQL_DATABASE=${MYSQL_DATABASE}
    networks:
      - db

volumes:
  db-data:
    name: db-data
    driver: local

networks:
  db:
    driver: bridge
